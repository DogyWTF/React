const Error = () => {
  return (
    <h1
      style={{
        width: "800px",
        textAlign: "center",
        marginTop: "50px",
        color: "red",
      }}
    >
      You went to a non-existent page!
    </h1>
  );
};

export default Error;
